# flask-wtforms-login-app

Project Summary (Concepts Used)
  - Jinja2 templating with template inheritance (base.html, extends, block, super())
  - WTForms + Flask‑WTF for form handling and validation
  - CSRF protection via SECRET_KEY
  - Simple auth logic with form validation and conditional routing
  - Static HTML/CSS styling inside templates
